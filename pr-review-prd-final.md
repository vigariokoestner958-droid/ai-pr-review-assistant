# PRD：X-Reviewer（Vibe Coder Edition）

**版本：** v1.1（PRD Development 正式版）
**日期：** 2026-05-29
**状态：** 待开发
**作者：** THC
**输出形态：** GitHub App

---

## 1. 执行摘要

我们为 **Vibe Coder（依赖 AI 辅助编程的独立开发者）** 构建 **AI 驱动的 GitHub App PR Review 工具**，解决他们**无法识别 AI 生成代码中的隐患、不会写有价值 Review 评论**的问题，从而让他们**有信心合并 PR，同时逐步提升自身 Review 能力**。

该工具安装后在 PR 提交时自动触发，在 10 分钟内生成结构化 Review 评论，包含变更摘要、分级风险提示（附教育性解释）和可一键采纳的代码修复建议。核心成功标准：用户对 AI 建议的"有帮助"标记率超过 **70%**。

---

## 2. 问题陈述

### 2.1 谁有这个问题？

**主要用户：Vibe Coder**
- 使用 Cursor、Copilot、Claude 等 AI 工具生成大量代码
- 能跑通项目，但缺乏代码审查的知识与经验
- 独立开发者、开源贡献者、个人项目作者
- 技术水平：入门级（几乎全靠 AI）～ 初中级（能读懂代码，但缺安全/架构经验）

### 2.2 问题是什么？

Vibe Coder 在合并 PR 时面临四层能力缺口：

| 缺口 | 具体表现 |
|------|---------|
| **不知看什么** | 没有 Review checklist，不知道关注哪里 |
| **看不懂风险** | 能读懂逻辑，识别不出安全/性能/边界问题 |
| **不会表达** | 发现问题也不知道怎么写有建设性的 Review 评论 |
| **缺乏标准** | 没有团队规范，不知道什么是"好代码" |

### 2.3 为什么痛？

- **直接后果：** 带着隐患合并代码，生产环境出 bug，紧急修复耗时耗力
- **心理后果：** 对 AI 生成的代码没有信心，要么焦虑合并，要么根本不 Review 直接合并
- **能力后果：** 没有反馈回路，Review 能力无法随使用积累提升

### 2.4 证据

- **个人亲身经历：** 作者本人是 Vibe Coder，深度体验过"不知道 AI 生成的代码有没有问题"的焦虑
- **社区观察：** GitHub / Reddit / Discord 中频繁出现"如何 Review AI 生成的代码"类讨论；Vibe Coding 相关社区中大量"PR 合并之后才发现 bug"的案例分享
- **行业趋势：** 2025年 AI 代码生成工具（Cursor、GitHub Copilot）用户量爆发式增长，产生代码量远超人工 Review 能力上限

### 2.5 现有方案的不足

| 方案 | 解决了什么 | 解决不了什么 |
|------|-----------|-------------|
| CI/Lint（ESLint、SonarQube） | 格式、静态语法问题 | 架构风险、业务逻辑错误、上下文理解 |
| 手动问 ChatGPT | 可以分析代码 | 操作繁琐、无项目上下文、输出无结构、没有行号定位 |
| CodeRabbit 等专业工具 | 针对有经验开发者的 bug 检查 | 未针对 Vibe Coder 优化，缺乏教育性解释，不解释"为什么" |

**核心差异化：** 现有工具告诉你"哪里有问题"，本工具告诉你"为什么是问题 + AI 生成代码的惯性陷阱 + 怎么一键修复"。

---

## 3. 目标用户与画像

### 3.1 主要画像：Vibe Coder（入门～初中级）

```
姓名：小明（代表性 Vibe Coder）
年龄：22-35 岁
背景：产品经理/设计师/非CS专业转行，或刚入行的初级开发者
工具链：Cursor + Claude + GitHub
项目类型：个人 SaaS 工具、开源小项目、副业项目
代码产出：大量（AI 辅助，每天可产出数百行）
Review 行为：跳过或走马观花，主要依赖 CI 绿灯
核心痛点：不知道 AI 代码里藏了什么坑，合并后怕出问题

入门级特征（约 40%）：
- 几乎全靠 AI 写代码
- 读不懂复杂技术解释
- 需要非常口语化、类比式的说明

初中级特征（约 60%）：
- 能读懂代码逻辑
- 缺乏安全/性能/架构的系统知识
- 能理解技术术语，但需要背景解释
```

### 3.2 次要画像：有经验的独立开发者

```
背景：3-5 年经验，有 Review 习惯
需求：提升 Review 效率，减少遗漏
与主要画像的区别：
- 不需要"为什么"的解释
- 更关注高精度、低误报
- 可能会关闭教育性解释
```

---

## 4. 战略背景

### 4.1 业务目标

| 目标类型 | 具体目标 |
|---------|---------|
| **短期（课程/竞赛）** | 展示完整的产品思维：发现→设计→实现，作为作品集 |
| **中期（个人工具）** | 解决自己作为 Vibe Coder 的真实痛点，自用驱动迭代 |
| **长期（商业化）** | 构建付费用户基础，探索 Pay-as-you-go 或订阅模式 |

### 4.2 市场机会

**为什么是现在？**

> Vibe Coding 在 2025 年爆发，大量非传统开发者开始用 AI 生产代码，但 Review 工具生态还停留在"服务有经验开发者"的阶段。这是一个 **12-18 个月的窗口期**——在大厂（GitHub、JetBrains）填补这个垂直场景之前。

**竞争定位：**
```
竞品（CodeRabbit / GitHub Copilot Review）
  → 面向有经验开发者
  → 找 bug + 规范检查
  → 告诉你"哪里有问题"

本工具
  → 面向 Vibe Coder（新兴、快速增长的细分市场）
  → 防翻车 + 架构把控 + 教育性成长
  → 告诉你"为什么是问题 + 怎么修 + AI 代码的惯性陷阱"
```

---

## 5. 方案概述

### 5.1 产品形态

**GitHub App**：开发者在 GitHub Marketplace 安装，授权仓库后，每次有新 PR 或 Push 到 PR 时自动触发分析，在 PR 页面发布结构化 Review 评论。

### 5.2 核心工作流

```
开发者提交 PR（或推送新 commit）
        ↓
GitHub Webhook 触发 App
        ↓
上下文获取（GitHub API）
  ├── PR 元数据（标题、描述、作者、分支）
  ├── 变更文件列表 + diff/patch
  ├── 原始文件代码（理解变更前状态）
  └── 现有 Review 评论（避免重复）
        ↓
AI 双层分析
  ├── Layer 1（Haiku，~3s）：变更摘要 + 文件重要性排序
  └── Layer 2（Sonnet，~15s）：风险扫描 + 教育性解释 + Suggestion
        ↓
结构化评论发布（< 10 分钟）
        ↓
开发者反馈收集（有帮助 / 无用 按钮）
```

### 5.3 GitHub PR 评论格式

```markdown
## 🤖 X-Reviewer — 变更摘要

**改动范围：** 3 个文件，+127 / -43 行
**变更类型：** 重构（安全模块）
**核心改动：** 将 JWT 校验从中间件移至 Service 层，解决 token 刷新竞态条件
**影响范围：** auth.service.ts（核心）/ user.controller.ts / auth.middleware.ts

---

## ⚠️ 风险评估

### 🔴 HIGH — 需要修复后合并

**[安全] JWT Secret 未做存在性校验** · `auth.service.ts:45`

若 `JWT_SECRET` 环境变量未配置，`jsonwebtoken` 将使用 `undefined` 作为签名密钥，
任何伪造 token 均可通过验证。这是 **AI 生成认证代码的高频陷阱**：
AI 会生成"能跑通"的认证逻辑，但往往省略生产环境必须的防御性校验。

```suggestion
const secret = process.env.JWT_SECRET;
if (!secret) throw new Error('JWT_SECRET environment variable is required');
const token = jwt.sign(payload, secret, { expiresIn: '1h' });
```

---

### 🟡 MEDIUM — 建议修复

**[性能] N+1 查询问题** · `user.controller.ts:89`

循环内调用 `await db.findUser()` 会导致每次请求产生 N 条独立查询。
在并发场景下会迅速耗尽数据库连接池。

```suggestion
// 将 userIds 收集后，改为批量查询
const users = await db.findUsers({ id: { in: userIds } });
```

---

## 📊 总体评估

| 维度 | 评分 |
|------|------|
| 安全性 | ⚠️ 需修复 |
| 性能 | 🟡 可改进 |
| 可维护性 | ✅ 良好 |
| 测试覆盖 | 🟡 建议补充 |

**建议：** 修复 HIGH 风险后可合并。

---
*对此 Review 有帮助吗？[👍 有帮助](#) · [👎 不准确](#) · [🔄 重新分析](#)*
```

---

## 6. 成功指标

### 6.1 核心指标（v1.0 必达）

| 指标 | 当前基线 | v1.0 目标 | 测量方式 |
|------|---------|----------|---------|
| **分析准确率** | N/A（新产品） | **>70% "有帮助"率** | PR 评论反馈按钮 |

> **为什么准确率是第一指标：**
> AI Review 机器人最大的风险是产生"噪音"打断开发者心流。只有建议足够准确，用户才会信任并保留 App。高准确率是高留存和高传播的前提，是所有其他指标的基础。

### 6.2 次要指标（监控但不优化）

| 指标 | 目标 | 说明 |
|------|------|------|
| 分析速度 P90 | < 10 分钟 | 不阻塞开发工作流 |
| GitHub App 安装量 | 50+ 仓库（Beta 阶段） | 传播力衡量 |
| 7 日留存（仓库维度） | > 60% | 安装后持续使用 |
| Suggestion 采纳率 | > 30% | 一键修复的实际使用率 |

### 6.3 护栏指标（不能变差）

| 指标 | 护栏线 | 说明 |
|------|--------|------|
| 误报率 | < 20% | "无用"标记率超过此值需立即调优 |
| 分析失败率 | < 5% | App 崩溃/超时/无响应 |
| 漏报率（HIGH 风险） | < 10% | 重大安全/性能问题被遗漏 |

---

## 7. 用户故事与验收标准

### 7.1 Epic 假设

> 我们相信，为 Vibe Coder 提供一个安装即用的 GitHub App，在 PR 提交后自动生成**高准确率的结构化 Review 评论**（含变更摘要、分级风险、教育性解释、一键采纳的 Suggestion 代码块），将使开发者**有信心合并 PR**，并通过持续使用**逐步提升 Review 能力**。我们通过"有帮助"标记率 > 70% 来验证此假设。

---

### 7.2 用户故事

---

**Story 1：自动触发分析**

> 作为开发者，当我提交 PR 或推送新 commit 时，我希望 App 在 10 分钟内自动分析并发布 Review 评论，这样我不需要做任何额外操作。

**验收标准：**
- [ ] PR 创建后，Bot 在 10 分钟内发布评论（P90）
- [ ] PR 推送新 commit 后，Bot 自动更新或新增评论
- [ ] 若分析超时（> 15 分钟），Bot 发布"分析超时，请手动触发"的提示
- [ ] Draft PR 默认不触发分析（除非用户配置开启）

---

**Story 2：变更摘要自动生成**

> 作为开发者（尤其是不爱写 PR Description 的 Vibe Coder），我希望评论顶部自动生成清晰的变更摘要，这样 Reviewer 和我自己都能快速理解这个 PR 做了什么。

**验收标准：**
- [ ] 评论首屏展示：改动文件数、增删行数、变更类型（新功能/重构/bug 修复/依赖升级）
- [ ] 核心改动描述：一句话说明"做了什么"
- [ ] 影响范围：列出核心被改动的模块/文件
- [ ] 若 PR Description 已存在，摘要补充而非重复

---

**Story 3：风险分级提示（含教育性解释）**

> 作为入门级 Vibe Coder，我希望风险提示不只告诉我"哪里有问题"，还告诉我"为什么是问题"，这样我能真正学到东西，不只是机械修复。

**验收标准：**
- [ ] 风险按 HIGH / MEDIUM / LOW 三级展示
- [ ] 每条风险包含：文件名 + 行号 + 风险类型标签 + 一句话描述
- [ ] HIGH/MEDIUM 风险附带教育性解释（≤ 3 句，口语化，解释根因）
- [ ] 标注"AI 生成代码高频陷阱"（当识别到典型 AI 代码模式时）
- [ ] LOW 风险默认折叠（`<details>` 标签），不干扰主要信息

---

**Story 4：一键采纳修复（GitHub Suggestion）**

> 作为开发者，我希望 AI 提供的修复建议可以直接在 PR 页面一键采纳，而不是手动复制粘贴，这样修复的摩擦感极低。

**验收标准：**
- [ ] HIGH/MEDIUM 风险中，至少 80% 附带 GitHub Suggestion 代码块（` ```suggestion ``` `）
- [ ] Suggestion 代码可直接在 GitHub PR 页面点击"Apply suggestion"采纳
- [ ] Suggestion 代码语法正确，可直接应用不引入新错误
- [ ] 无法生成 Suggestion 时（如架构级问题），提供文字修复指南

---

**Story 5：反馈收集**

> 作为开发者，我希望能快速对 AI 的 Review 建议打分，这样工具能持续改进，我也能表达"这条没用"。

**验收标准：**
- [ ] 评论底部展示：`[👍 有帮助] [👎 不准确]` 可点击链接
- [ ] 点击后跳转轻量反馈页（或 GitHub Issue 模板），记录 PR URL + 反馈类型
- [ ] 反馈数据进入准确率统计看板（内部）

---

**Story 6：手动重新触发**

> 作为开发者，当我修复了 AI 指出的问题后，我希望能通过评论命令触发重新分析，而不需要推送新 commit。

**验收标准：**
- [ ] 在 PR 评论中输入 `@pr-review-bot re-review`，Bot 在 10 分钟内发布更新后的分析
- [ ] 重新分析基于当前最新 commit，不是历史版本
- [ ] Bot 回复确认："✅ 已收到，正在重新分析..."

---

### 7.3 约束与边界条件

**PR 规模限制：**
- 超过 **2000 行 diff** 或 **100 个文件** 的 PR：拒绝分析，评论说明原因
- 建议开发者拆分 PR（附简短的拆分建议）
- 理由：避免触发 LLM Token 限制；大型 PR 不符合 Vibe Coding "小步快跑"最佳实践

**支持的语言（v1.0）：**
- JavaScript / TypeScript（优先，Vibe Coder 主力语言）
- Python
- 其他语言降级处理（通用分析，不做语言专项优化）

**安全性约束：**
- 代码仅在分析时传输，不持久化存储
- 支持 Private Repository（通过 GitHub App Installation Token）
- 不分析 `.env`、密钥文件（在配置中排除）

---

## 8. 范围边界（Out of Scope）

### v1.0 明确不做

| 排除项 | 原因 |
|--------|------|
| **GitLab / Bitbucket 支持** | 先在 GitHub 验证核心价值，再扩展平台 |
| **团队级 Web Dashboard** | 增加复杂度，MVP 专注核心分析能力 |
| **企业私有化部署（Self-host）** | 合规/安全需求复杂，非 Vibe Coder 目标场景 |
| **CI/CD 阻塞集成（blocking check）** | 误报风险高，在准确率未达标前不做 |
| **超大 PR 分析（>2000行/100文件）** | Token 限制 + 违背 Vibe Coding 最佳实践 |
| **实时代码补全** | Copilot 的领域，避免能力重叠 |
| **多语言 UI** | 专注核心功能，国际化后续规划 |

### v2.0 规划（暂不承诺）

- IDE 插件（VS Code）：提 PR 前本地 Review
- GitLab / Bitbucket 支持
- 团队规范学习（上传 coding guidelines）
- 历史 PR 记忆（避免重复犯同类错误）

---

## 9. 技术方案

### 9.1 系统架构

```
GitHub Webhook（PR events）
        ↓
App Server（接收事件，入队）
        ↓
Analysis Worker（异步处理）
  ├── GitHub API Client
  │   ├── 获取 PR 元数据
  │   ├── 获取 diff/patch（按文件）
  │   └── 获取原始文件内容（未修改版本）
  ├── Context Builder（上下文构建器）
  │   ├── 小 PR（<300行）：完整 diff 直接送入
  │   ├── 中型 PR（300-2000行）：按文件分块并行分析后合并
  │   └── 超大 PR（>2000行）：拒绝，返回拆分建议
  └── AI Analyzer（双层模型）
      ├── Layer 1（Haiku）：摘要生成 + 文件重要性排序（~3s）
      └── Layer 2（Sonnet）：风险扫描 + 教育性解释 + Suggestion 生成（~15s）
        ↓
Comment Publisher（GitHub API）
  ├── 生成 Markdown 格式评论
  ├── 嵌入 GitHub Suggestion 代码块
  └── 发布为 PR Review Comment
        ↓
Feedback Collector（准确率统计）
```

### 9.2 模型选择策略

| 任务 | 模型 | 原因 |
|------|------|------|
| 变更摘要、文件分类、重要性排序 | `claude-haiku-4-5-20251001` | 速度快（<3s）、成本低，结构化提取任务效果好 |
| 风险识别、教育性解释、Suggestion 生成 | `claude-sonnet-4-6` | 推理能力强，能理解复杂代码语义和安全模式 |
| 架构级综合评估（可选，大 PR 关键文件） | `claude-opus-4-6` | 最强上下文理解，按需调用控制成本 |

**降级策略：** 当单文件 diff 超过 50k tokens 时，自动截断并标注"文件过大，已做部分分析"。

### 9.3 上下文获取策略

**上下文优先级（高→低）：**

1. **PR Description**：开发者意图，最关键的上下文信号
2. **核心变更文件完整 diff**：实际代码变更
3. **被修改函数/类的原始代码**（通过 GitHub Contents API 获取变更前版本）
4. **同文件其他函数签名**（理解代码结构和调用关系）
5. **PR 标题 + Labels**：补充意图分类
6. **现有 Review 评论**：避免重复已有建议

**误报控制机制：**

| 机制 | 实现方式 |
|------|---------|
| 证据要求 | System Prompt 要求"只报告有具体代码证据的问题，不做推测" |
| 位置锁定 | 每条风险必须包含文件名 + 行号，无法定位则不报告 |
| 置信度筛选 | 低置信度建议标记 `[?]`，不计入 HIGH/MEDIUM |
| 用户反馈回路 | "👎 不准确"反馈触发人工复查队列，用于 Prompt 调优 |

### 9.4 技术栈

```
后端：Python 3.11+ / Node.js（待定）
AI SDK：anthropic（官方 SDK）
GitHub 集成：GitHub Apps + Webhooks + REST API v3
部署：Railway / Render（轻量，快速迭代）
数据库：PostgreSQL（安装记录、反馈数据）
队列：Redis + RQ（异步任务处理）
```

---

## 10. 依赖与风险

### 10.1 依赖项

| 依赖 | 类型 | 说明 |
|------|------|------|
| Anthropic API | 外部服务 | 核心 AI 能力，需 API Key |
| GitHub Apps 审核 | 外部流程 | GitHub Marketplace 上架需要审核（非阻塞，可先通过直接安装链接使用）|
| GitHub REST API | 外部服务 | Rate Limit：5000 req/hour（Installation Token），足够 MVP 使用 |

### 10.2 风险与缓解

| 风险 | 概率 | 影响 | 缓解策略 |
|------|------|------|---------|
| 误报率过高导致用户卸载 | 中 | 高 | 严格 System Prompt + 用户反馈 + 持续 Prompt 调优；Beta 阶段小规模测试 |
| GitHub 推出官方 AI Review 功能 | 高 | 高 | 深耕 Vibe Coder 教育性体验，大厂难以快速优化垂直场景 |
| Anthropic API 成本超预期 | 中 | 中 | 双层模型策略（Haiku 处理简单任务）；大 PR 限制避免超长 context |
| GitHub API Rate Limit | 低 | 中 | 实现请求缓存；引导用户使用较低频率的 Webhook 触发策略 |

---

## 11. 开放问题

| 问题 | 状态 | 截止 |
|------|------|------|
| GitHub App 名称和 Marketplace 描述 | 待定 | 开发前确认 |
| 反馈收集机制：GitHub Issue 还是独立表单？ | 待讨论 | Sprint 1 |
| 针对入门级用户的教育性解释是否需要中英文切换？ | 待定 | v1.1 |
| 是否支持用户自定义排除规则（.pr-review-ignore）？ | 倾向支持 | v1.1 |
| Beta 阶段如何控制 API 成本（每 PR 分析成本约 $0.02-$0.05）？ | 需要预算评估 | Sprint 1 |

---

## 附录：未来扩展方向

### v2.0（3–6 个月）

- **IDE 集成（VS Code 插件）**：提 PR 前本地运行，零摩擦防翻车
- **GitLab / Bitbucket 支持**：扩展非 GitHub 平台用户
- **团队规范学习**：上传 `.coding-standards.md`，AI 基于规范 Review
- **历史记忆**：记录该仓库过往问题模式，避免重复犯错

### v3.0（6–12 个月）

- **CI/CD 集成（在准确率达标后）**：作为 GitHub Actions 可选阻塞步骤
- **项目级架构理解**：索引整个仓库，全局上下文 Review
- **Vibe Coding 成长报告**：分析开发者的代码模式，给出能力提升路线
- **Enterprise**：私有化部署、合规审计、SAML SSO

---
