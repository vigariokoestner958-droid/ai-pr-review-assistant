# AI PR Review 助手 — 系统设计说明

> 面向竞赛评审的技术设计文档
> 覆盖：模型选择 · 上下文获取 · 误报控制 · 未来扩展

---

## 一、产品背景

**目标用户：Vibe Coder**——依赖 Cursor / Copilot / Claude 生成大量代码，但缺乏识别 AI 代码隐患的经验。他们面临的核心痛点不是"没有 Review 工具"，而是：

- 现有工具（CodeRabbit、SonarQube）告诉你**哪里有问题**
- 本工具告诉你**为什么是问题 + AI 生成代码的惯性陷阱 + 怎么一键修复**

**核心成功指标：** 用户对 AI 建议的"有帮助"标记率 > 70%

---

## 二、系统架构总览

```
用户输入 PR URL
      │
      ▼
┌─────────────────┐
│   Web Frontend  │  ← 输入框 + 结果展示
└────────┬────────┘
         │ POST /analyze
         ▼
┌─────────────────┐
│   FastAPI       │  ← api.py
│   后端服务      │
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
GitHub API   AI 分析引擎
Client       (双层模型)
    │         │
    │    ┌────┴─────────────────┐
    │    │ Layer 1: Haiku (~3s) │  摘要生成
    │    │ Layer 2: Sonnet(~15s)│  风险扫描
    │    └─────────────────────┘
    │         │
    └────┬────┘
         ▼
  结构化 JSON 结果
         │
    ┌────┴────┐
    ▼         ▼
前端展示   GitHub PR
         发布评论
              │
              ▼
        反馈收集系统
       (👍/👎 → SQLite)
```

---

## 三、模型选择策略

### 3.1 为什么选 Claude？

| 维度 | 说明 |
|------|------|
| **代码理解深度** | Claude Sonnet 在代码语义理解和安全模式识别上表现优于同级竞品 |
| **指令遵从** | 严格遵守"只报告有证据的问题"的约束，误报率更低 |
| **结构化输出** | JSON 格式稳定，解析失败率极低 |
| **中文表达** | 教育性解释的口语化中文输出质量高，适合 Vibe Coder 受众 |

### 3.2 双层模型架构

```
PR 上下文
    │
    ├──▶ Layer 1: claude-haiku-4-5-20251001（速度优先）
    │         任务：变更摘要 + 文件重要性排序
    │         延迟：~3 秒
    │         成本：约 $0.003/次
    │         原因：结构化提取任务，不需要强推理；快速响应改善体验
    │
    └──▶ Layer 2: claude-sonnet-4-6（质量优先）
              任务：风险识别 + 教育性解释 + Suggestion 生成
              延迟：~15 秒
              成本：约 $0.015/次
              原因：复杂代码语义理解、安全模式推理需要更强的模型
```

**设计原则：** 用廉价快速的模型处理结构化任务，用强力模型处理需要推理的任务，总成本控制在 **$0.02-0.05/次 PR 分析**。

### 3.3 降级策略

| 场景 | 处理方式 |
|------|---------|
| 单文件 diff 超 50k tokens | 截断并标注"文件过大，已做部分分析" |
| PR 超 2000 行 / 100 文件 | 拒绝分析，返回拆分建议 |
| API 超时（> 30s） | 返回已完成部分，标注未完成 |
| Sonnet 不可用 | 降级为纯 Haiku 分析，标注"简化模式" |

---

## 四、上下文获取策略

### 4.1 上下文来源与优先级

```
优先级（高 → 低）

1. PR Description      ← 开发者意图，最关键的信号
2. 核心变更文件 diff   ← 实际代码变更（patch/hunk）
3. 原始文件内容        ← 理解变更前的状态（via Contents API）
4. 同文件其他函数签名  ← 理解调用关系和代码结构
5. PR 标题 + Labels   ← 补充意图分类
6. 现有 Review 评论   ← 避免重复已有建议
```

### 4.2 上下文构建逻辑

```python
# 按 PR 规模采用不同策略
if total_lines < 300:
    # 小型 PR：完整 diff 直接送入，上下文最完整
    context = full_diff + file_contents

elif total_lines < 2000:
    # 中型 PR：按文件分块，优先分析高风险文件
    files_sorted = sort_by_importance(files)  # Layer 1 排序结果
    context = top_files_diff + summary_of_rest

else:
    # 超大 PR：拒绝，建议拆分
    return reject_with_split_suggestion()
```

### 4.3 为什么不用 RAG / 向量检索？

MVP 阶段选择**直接传入 diff + 原始文件**而非 RAG，原因：

| 维度 | 直接传入（当前） | RAG（未来） |
|------|----------------|------------|
| 实现复杂度 | 低 | 高 |
| 延迟 | 低（无检索步骤） | 中（需向量检索）|
| 适用场景 | PR 级别（局部变更）| 仓库级别（全局理解）|
| 准确性 | 对 PR 范围内代码准确 | 能理解跨文件依赖 |

**结论：** PR Review 的核心场景是"理解这次变更"，而非"理解整个仓库"，直接传入 diff 在大多数情况下已经足够。仓库级别的全局理解是 v2.0 规划项。

---

## 五、误报与漏报控制

### 5.1 误报控制机制

AI Review 最大的风险是产生"噪音"打断开发者心流，导致用户卸载。

| 机制 | 实现方式 |
|------|---------|
| **证据锁定** | System Prompt 强制要求"只报告有具体代码证据的问题，不做推测性建议" |
| **位置锁定** | 每条风险必须包含文件名 + 行号，无法定位则不输出 |
| **置信度筛选** | LOW 级别问题设置更高的输出门槛，避免无效噪音 |
| **反馈回路** | 👎"不准确"标记触发人工复查队列，用于 Prompt 持续调优 |

**核心 System Prompt 约束（节选）：**
```
规则：
1. 只报告有具体代码证据的问题，不做推测性建议
2. 每条风险必须有文件名和行号（如无法确定行号，写 null）
3. 不要制造"噪音"：LOW 以下的风格建议不要报告
```

### 5.2 漏报控制机制

| 机制 | 实现方式 |
|------|---------|
| **Vibe Coding 专项模式库** | 针对 AI 生成代码的高频陷阱（SQL拼接、shell=True、硬编码密钥等）在 Prompt 中显式列举 |
| **分类覆盖** | 5类风险（安全/性能/正确性/架构/可维护性）确保扫描维度完整 |
| **原始文件上下文** | 获取变更前的代码，避免因只看 diff 而遗漏上下文相关问题 |

### 5.3 实测准确性数据

基于 5 个测试 PR（含 20 个预埋漏洞）的验证结果：

| 指标 | 结果 |
|------|------|
| 预埋漏洞检测率 | 20/20 = **100%** |
| 误报数（无效问题）| 0 个 |
| 额外发现有效问题 | 1 个（PR#2 localStorage token 存储）|
| 评分区分度 | 干净代码 8/10，漏洞代码 2-4/10，分布合理 |

> 测试样本覆盖：命令注入、XSS、SQL注入、明文密码存储、N+1查询、内存泄露等6类典型漏洞。详见 `test_results/2026-05-29_test_report.md`

---

## 六、响应速度优化

| 优化点 | 实现方式 | 效果 |
|--------|---------|------|
| 双层并行潜力 | Layer 1 结果不阻塞 Layer 2 启动（未来优化）| 减少 3s |
| Diff 截断 | 超长文件自动截断，避免超时 | P99 < 30s |
| FastAPI 异步 | `async/await` 全链路异步 | 高并发不阻塞 |
| 评论预览 | dry-run 模式本地即时展示 | 无需等待 GitHub API |

**实测延迟：** Layer 1 ~3s + Layer 2 ~15s = **总计 ~18s**（网络环境下），满足 P90 < 10分钟的指标。

---

## 七、使用体验设计

### 7.1 三种使用入口

| 入口 | 场景 | 状态 |
|------|------|------|
| **CLI** (`python main.py <PR_URL>`) | 开发者本地快速验证 | ✅ 已实现 |
| **Web 前端** (http://localhost:8000) | 可视化展示 + Demo | ✅ 已实现 |
| **GitHub App Webhook** | 提交PR自动触发 | 🔶 规划中 |

### 7.2 评论格式设计原则

- **首屏信息密度：** 变更摘要 + 评分 + 关键风险，3秒内获取核心信息
- **渐进式展开：** LOW 风险默认折叠，减少视觉噪音
- **可执行性：** 每条 HIGH/MEDIUM 风险附带可直接采纳的 `suggestion` 代码块
- **教育性：** "为什么是问题"解释口语化，面向缺乏安全/性能背景的 Vibe Coder

---

## 八、未来扩展方向

### 8.1 短期（v1.1，1-3 个月）

**GitHub App 自动化**
- 注册正式 GitHub App，通过 Webhook 在 PR 提交时自动触发
- 支持 `@pr-review-bot re-review` 命令重新触发
- 安装后零配置，开箱即用

**准确率调优循环**
- 基于反馈数据（👍/👎）持续优化 System Prompt
- 目标：将"有帮助"率从当前 ~80%（测试样本）推至真实用户 >70%
- A/B 测试不同 Prompt 策略

### 8.2 中期（v2.0，3-6 个月）

**仓库级上下文理解**
- 索引整个仓库，理解跨文件依赖关系
- 技术方案：tree-sitter 解析 AST + 向量索引核心函数签名
- 效果：识别"A 文件修改影响 B 文件调用链"类的深层问题

**团队规范学习**
- 上传 `.coding-standards.md`，AI 基于团队规范 Review
- 解决"什么是好代码因团队而异"的痛点

**IDE 插件（VS Code）**
- 提 PR 前本地运行，零摩擦防翻车
- 在写代码时实时提示，而非 PR 后补救

### 8.3 长期（v3.0，6-12 个月）

**Vibe Coding 成长报告**
- 分析开发者跨 PR 的代码模式变化
- 生成个人能力提升路线图：「你最近3次PR都出现SQL注入，推荐学习参数化查询」

**CI/CD 集成（在准确率 >90% 后）**
- 作为 GitHub Actions 可选阻塞步骤
- HIGH 风险 PR 自动阻止合并

**Enterprise**
- 私有化部署、合规审计
- SAML SSO、团队维度分析看板

---

## 九、技术栈总结

| 层级 | 选型 | 选择理由 |
|------|------|---------|
| AI 分析 | Anthropic Claude（Haiku + Sonnet）| 代码理解深度 + 指令遵从性 |
| 后端 | Python + FastAPI | 轻量快速，异步支持好 |
| GitHub 集成 | GitHub REST API v3 | 官方支持，Rate Limit 足够 |
| 数据持久化 | SQLite（反馈数据）| MVP 阶段零运维成本 |
| 部署 | localhost + ngrok（Demo）/ Railway（生产）| 快速迭代 |

---

*本文档基于 PRD v1.1 + 工程化执行文档，结合实际测试数据撰写。*
*测试数据详见：`test_results/2026-05-29_test_report.md`*
